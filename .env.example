# ═══════════════════════════════════════════════════════════
# APP
# ═══════════════════════════════════════════════════════════
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# ═══════════════════════════════════════════════════════════
# MODE
# ═══════════════════════════════════════════════════════════
# fork       = Fast cycles (~20 min) on Anvil fork for testing
# production = Weekly cron schedule on Base mainnet
MODE=fork

# ═══════════════════════════════════════════════════════════
# FORK MODE SCHEDULING - Two Options
# ═══════════════════════════════════════════════════════════
#
# OPTION A: Cron-based (recommended for testing at specific times)
#   Set SNAPSHOT_CRON and CALCULATE_CRON with cron expressions
#   Airdrop is always manual (admin enters reward amount)
#
# OPTION B: Interval-based (for quick testing)
#   Leave cron vars empty, set AUTO_START=true
#   Uses setTimeout intervals between steps
#
# ═══════════════════════════════════════════════════════════

# --- Option A: Cron-based scheduling ---
# Cron format: "minute hour day month weekday"
# Examples:
#   "30 14 * * *"   = Every day at 2:30 PM
#   "0 */2 * * *"   = Every 2 hours
#   "*/5 * * * *"   = Every 5 minutes
#   "0 9,17 * * *"  = At 9 AM and 5 PM daily
#
# SNAPSHOT_CRON=30 14 * * *
# CALCULATE_CRON=45 14 * * *

# --- Option B: Interval-based scheduling ---
AUTO_START=false                # true = start on server boot
START_DELAY_MINUTES=0           # Minutes to wait before starting
SNAPSHOT_INTERVAL=10            # Minutes between START and END snapshots
CALCULATE_DELAY=5               # Minutes after END snapshot to calculate
AIRDROP_DELAY=5                 # Minutes after calculation (if auto-approve)

# ═══════════════════════════════════════════════════════════
# MOCK FLAGS
# ═══════════════════════════════════════════════════════════
# MOCK_SNAPSHOTS:    true = fake holder data, false = real Moralis API
# MOCK_TRANSACTIONS: true = simulate tx, false = real blockchain
#
# Recommended combinations:
#   Development:     MOCK_SNAPSHOTS=true,  MOCK_TRANSACTIONS=true
#   Fork Testing:    MOCK_SNAPSHOTS=false, MOCK_TRANSACTIONS=false (with Anvil RPC)
#   Production:      MOCK_SNAPSHOTS=false, MOCK_TRANSACTIONS=false (with mainnet RPC)
MOCK_SNAPSHOTS=false
MOCK_TRANSACTIONS=false

# ═══════════════════════════════════════════════════════════
# DATABASE
# ═══════════════════════════════════════════════════════════
MONGODB_URI=mongodb://localhost:27017/aquari-airdrop

# ═══════════════════════════════════════════════════════════
# REDIS (For BullMQ job queue - optional in development)
# ═══════════════════════════════════════════════════════════
REDIS_URL=redis://localhost:6379

# ═══════════════════════════════════════════════════════════
# ADMIN DASHBOARD CREDENTIALS
# KEEP THESE SECRET!
# ═══════════════════════════════════════════════════════════
ADMIN_USERNAME=aquari_admin
ADMIN_PASSWORD=your_super_secure_password_here_123!
SESSION_SECRET=random_64_char_string_for_session_encryption_abc123xyz

# ═══════════════════════════════════════════════════════════
# MORALIS API (Free Tier: 40,000 CU/day)
# ═══════════════════════════════════════════════════════════
MORALIS_API_KEY=your_moralis_api_key

# ═══════════════════════════════════════════════════════════
# BLOCKCHAIN
# ═══════════════════════════════════════════════════════════
# RPC_URL: The RPC endpoint for transactions
#   - For fork mode: http://localhost:8545 (Anvil)
#   - For production: https://mainnet.base.org (or your own RPC)
#
# Defaults based on MODE:
#   fork: http://localhost:8545
#   production: https://mainnet.base.org
RPC_URL=http://localhost:8545

# ═══════════════════════════════════════════════════════════
# WALLET
# ═══════════════════════════════════════════════════════════
# Required for production mode, auto-generated for fork mode
# PRIVATE_KEY=your_airdropper_wallet_private_key

# ═══════════════════════════════════════════════════════════
# TOKEN CONFIG
# ═══════════════════════════════════════════════════════════
# TOKEN_ADDRESS: The ERC20 token to airdrop
#   Default: AQUARI (0x7F0E9971D3320521Fc88F863E173a4cddBB051bA)
#   You can specify any ERC20 token address on Base
#
# TOKEN_SYMBOL: Display name for the token (optional, defaults to AQUARI)
# TOKEN_DECIMALS: Token decimals (optional, defaults to 18)
#
TOKEN_ADDRESS=0x7F0E9971D3320521Fc88F863E173a4cddBB051bA
TOKEN_SYMBOL=AQUARI
TOKEN_DECIMALS=18
MIN_BALANCE=1000000000000000000000   # 1000 tokens in wei (adjust for decimals)

# ═══════════════════════════════════════════════════════════
# BATCH CONFIG
# ═══════════════════════════════════════════════════════════
BATCH_SIZE=500                # Recipients per transaction (max ~500 for Base)
MAX_GAS_PRICE=50000000000     # 50 gwei max
CONFIRMATIONS=1               # Blocks to wait (1 for fork, 3 for production)

# ═══════════════════════════════════════════════════════════
# QUICK START EXAMPLES
# ═══════════════════════════════════════════════════════════
#
# 1. FORK + CRON (run snapshot at 2:30 PM, calculate at 2:45 PM):
#    MODE=fork
#    SNAPSHOT_CRON=30 14 * * *
#    CALCULATE_CRON=45 14 * * *
#    MOCK_SNAPSHOTS=false
#    MOCK_TRANSACTIONS=false
#    RPC_URL=http://localhost:8545
#
# 2. FORK + MANUAL START (trigger from dashboard):
#    MODE=fork
#    AUTO_START=false
#    MOCK_SNAPSHOTS=false
#    MOCK_TRANSACTIONS=false
#    RPC_URL=http://localhost:8545
#
# 3. FORK + AUTO-START with delay:
#    MODE=fork
#    AUTO_START=true
#    START_DELAY_MINUTES=5
#    SNAPSHOT_INTERVAL=10
#    CALCULATE_DELAY=5
#
# 4. PRODUCTION (weekly schedule, real transactions):
#    MODE=production
#    MOCK_SNAPSHOTS=false
#    MOCK_TRANSACTIONS=false
#    RPC_URL=https://mainnet.base.org
#    PRIVATE_KEY=your_real_private_key
