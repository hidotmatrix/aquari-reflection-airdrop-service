<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Snapshot <%= snapshot.weekId %> - AQUARI Airdrop Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('partials/nav') %>
  <main class="max-w-7xl mx-auto py-6 px-4">
    <div class="mb-6">
      <a href="/admin/snapshots" class="text-indigo-600 hover:underline">&larr; Back to Snapshots</a>
    </div>
    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h1 class="text-2xl font-bold mb-4">Snapshot: <%= snapshot.weekId %></h1>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div><span class="text-gray-500">Status:</span> <span class="font-medium"><%= snapshot.status %></span></div>
        <div><span class="text-gray-500">Total Holders:</span> <span class="font-medium"><%= snapshot.totalHolders?.toLocaleString() %></span></div>
        <div><span class="text-gray-500">Timestamp:</span> <span class="font-medium"><%= new Date(snapshot.timestamp).toLocaleString() %></span></div>
        <div><span class="text-gray-500">API Calls:</span> <span class="font-medium"><%= snapshot.metadata?.apiCallCount || 0 %></span></div>
      </div>
    </div>
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <div class="px-6 py-4 border-b"><h2 class="text-lg font-medium">Holders (Top by Balance)</h2></div>
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Address</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Balance</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Label</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% holders.forEach(h => { %>
          <tr>
            <td class="px-6 py-4 font-mono text-sm"><%= h.address %></td>
            <td class="px-6 py-4"><%= h.balanceFormatted %></td>
            <td class="px-6 py-4 text-sm text-gray-500"><%= h.label || h.entity || '-' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%- include('partials/pagination', { pagination }) %>
  </main>
</body>
</html>
