<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Distributions - AQUARI Airdrop Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('partials/nav') %>
  <main class="max-w-7xl mx-auto py-6 px-4">
    <h1 class="text-2xl font-bold mb-6">Distributions</h1>
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Week</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Eligible</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Reward Pool</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Created</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% distributions.forEach(d => { %>
          <tr>
            <td class="px-6 py-4 font-medium"><%= d.weekId %></td>
            <td class="px-6 py-4"><span class="px-2 py-1 text-xs rounded <%= d.status === 'completed' ? 'bg-green-100 text-green-800' : d.status === 'failed' ? 'bg-red-100 text-red-800' : 'bg-yellow-100 text-yellow-800' %>"><%= d.status %></span></td>
            <td class="px-6 py-4"><%= d.stats?.eligibleHolders?.toLocaleString() || 0 %></td>
            <td class="px-6 py-4"><%= d.config?.rewardPool ? (BigInt(d.config.rewardPool) / BigInt(10**18)).toString() + ' ' + d.config.rewardToken : '-' %></td>
            <td class="px-6 py-4 text-sm text-gray-500"><%= new Date(d.createdAt).toLocaleString() %></td>
            <td class="px-6 py-4"><a href="/admin/distributions/<%= d._id %>" class="text-indigo-600 hover:underline">View</a></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%- include('partials/pagination', { pagination }) %>
  </main>
</body>
</html>
