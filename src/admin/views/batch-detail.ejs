<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Batch #<%= batch.batchNumber %> - AQUARI Airdrop Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('partials/nav') %>
  <main class="max-w-7xl mx-auto py-6 px-4">
    <div class="mb-6"><a href="/admin/batches" class="text-indigo-600 hover:underline">&larr; Back to Batches</a></div>
    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h1 class="text-2xl font-bold mb-4">Batch #<%= batch.batchNumber %> - <%= batch.weekId %></h1>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div><span class="text-gray-500">Status:</span> <span class="font-medium"><%= batch.status %></span></div>
        <div><span class="text-gray-500">Recipients:</span> <span class="font-medium"><%= batch.recipientCount %></span></div>
        <div><span class="text-gray-500">Retries:</span> <span class="font-medium"><%= batch.retryCount %>/<%= batch.maxRetries %></span></div>
        <% if (batch.execution) { %>
        <div><span class="text-gray-500">Gas Used:</span> <span class="font-medium"><%= batch.execution.gasUsed %></span></div>
        <% } %>
      </div>
      <% if (batch.execution?.txHash) { %>
      <div class="mt-4">
        <span class="text-gray-500">Transaction:</span>
        <a href="https://basescan.org/tx/<%= batch.execution.txHash %>" target="_blank" class="text-indigo-600 hover:underline font-mono text-sm ml-2"><%= batch.execution.txHash %></a>
      </div>
      <% } %>
      <% if (batch.lastError) { %>
      <div class="mt-4 p-3 bg-red-50 rounded text-red-700 text-sm"><%= batch.lastError %></div>
      <% } %>
    </div>
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <div class="px-6 py-4 border-b"><h2 class="text-lg font-medium">Recipients in Batch</h2></div>
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Address</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Amount</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% batch.recipients.forEach(r => { %>
          <tr>
            <td class="px-6 py-4 font-mono text-sm"><a href="/admin/search?address=<%= r.address %>" class="text-indigo-600 hover:underline"><%= r.address %></a></td>
            <td class="px-6 py-4"><%= (BigInt(r.amount) / BigInt(10**18)).toString() %> ETH</td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%- include('partials/pagination', { pagination }) %>
  </main>
</body>
</html>
